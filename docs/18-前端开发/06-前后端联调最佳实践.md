# 前后端联调最佳实践

## 一、API 设计规范

### 1.1 RESTful API 设计

```yaml
# RESTful 设计原则

# 1. 资源命名（名词，复数形式）
GET    /api/users          # 获取用户列表
GET    /api/users/123      # 获取特定用户
POST   /api/users          # 创建用户
PUT    /api/users/123      # 全量更新用户
PATCH  /api/users/123      # 部分更新用户
DELETE /api/users/123      # 删除用户

# 2. 嵌套资源
GET    /api/users/123/orders       # 获取用户的订单列表
GET    /api/users/123/orders/456   # 获取用户的特定订单
POST   /api/users/123/orders       # 为用户创建订单

# 3. 查询参数
GET    /api/users?page=1&size=20&sort=name,asc&filter=status:active

# 4. 版本控制
/api/v1/users
/api/v2/users

# 5. HTTP 状态码
200 OK              # 请求成功
201 Created         # 创建成功
204 No Content      # 删除成功，无返回体
400 Bad Request     # 请求参数错误
401 Unauthorized    # 未认证
403 Forbidden       # 无权限
404 Not Found       # 资源不存在
409 Conflict        # 资源冲突
422 Unprocessable   # 验证错误
500 Server Error    # 服务器错误
```

### 1.2 统一响应格式

```typescript
// 标准响应结构
interface ApiResponse<T> {
    code: number;           // 业务状态码
    message: string;        // 提示信息
    data: T;               // 响应数据
    timestamp: number;     // 时间戳
    traceId: string;       // 追踪 ID（用于日志排查）
}

// 分页响应结构
interface PageResponse<T> {
    list: T[];             // 数据列表
    total: number;         // 总记录数
    page: number;          // 当前页
    size: number;          // 每页大小
    pages: number;         // 总页数
}

// 错误响应结构
interface ErrorResponse {
    code: number;
    message: string;
    errors?: FieldError[]; // 字段级错误
    path: string;          // 请求路径
    traceId: string;
}

interface FieldError {
    field: string;         // 错误字段
    message: string;       // 错误信息
    rejectedValue: any;    // 被拒绝的值
}
```

```java
// Java 后端统一响应封装
@Data
public class Result<T> {
    private Integer code;
    private String message;
    private T data;
    private Long timestamp;
    private String traceId;
    
    public static <T> Result<T> success(T data) {
        Result<T> result = new Result<>();
        result.setCode(200);
        result.setMessage("success");
        result.setData(data);
        result.setTimestamp(System.currentTimeMillis());
        result.setTraceId(MDC.get("traceId"));
        return result;
    }
    
    public static <T> Result<T> error(String message) {
        Result<T> result = new Result<>();
        result.setCode(500);
        result.setMessage(message);
        result.setTimestamp(System.currentTimeMillis());
        return result;
    }
}
```

## 二、HTTP 请求封装

### 2.1 Axios 封装（前端）

```typescript
// utils/request.ts
import axios, { 
    AxiosInstance, 
    AxiosRequestConfig, 
    AxiosResponse,
    AxiosError 
} from 'axios';
import { ElMessage, ElMessageBox } from 'element-plus';
import { useUserStore } from '@/stores/user';

// 创建 axios 实例
const request: AxiosInstance = axios.create({
    baseURL: import.meta.env.VITE_API_BASE_URL,
    timeout: 30000,
    headers: {
        'Content-Type': 'application/json'
    }
});

// 请求队列（用于取消重复请求）
const pendingMap = new Map<string, AbortController>();

// 生成请求 key
const getRequestKey = (config: AxiosRequestConfig): string => {
    return `${config.method}_${config.url}_${JSON.stringify(config.params)}_${JSON.stringify(config.data)}`;
};

// 添加请求到队列
const addPending = (config: AxiosRequestConfig): void => {
    const key = getRequestKey(config);
    const controller = new AbortController();
    config.signal = controller.signal;
    pendingMap.set(key, controller);
};

// 移除请求
const removePending = (config: AxiosRequestConfig): void => {
    const key = getRequestKey(config);
    const controller = pendingMap.get(key);
    if (controller) {
        controller.abort();
        pendingMap.delete(key);
    }
};

// 请求拦截器
request.interceptors.request.use(
    (config: AxiosRequestConfig) => {
        // 1. 重复请求处理
        removePending(config);
        if (!config.headers?.['X-Allow-Duplicate']) {
            addPending(config);
        }
        
        // 2. 添加 Token
        const userStore = useUserStore();
        const token = userStore.token;
        if (token) {
            config.headers = config.headers || {};
            config.headers.Authorization = `Bearer ${token}`;
        }
        
        // 3. 添加时间戳（防止缓存）
        if (config.method === 'get') {
            config.params = {
                ...config.params,
                _t: Date.now()
            };
        }
        
        // 4. 添加 TraceId
        config.headers['X-Trace-Id'] = generateTraceId();
        
        return config;
    },
    (error: AxiosError) => {
        return Promise.reject(error);
    }
);

// 响应拦截器
request.interceptors.response.use(
    (response: AxiosResponse<ApiResponse<any>>) => {
        // 移除已完成请求
        removePending(response.config);
        
        const { data } = response;
        
        // 业务状态码处理
        if (data.code !== 200) {
            handleBusinessError(data);
            return Promise.reject(new Error(data.message));
        }
        
        return data.data;
    },
    (error: AxiosError<ErrorResponse>) => {
        // 移除已失败请求
        if (error.config) {
            removePending(error.config);
        }
        
        // 处理 HTTP 错误
        handleHttpError(error);
        
        return Promise.reject(error);
    }
);

// 业务错误处理
const handleBusinessError = (data: ApiResponse<any>): void => {
    switch (data.code) {
        case 401:
            ElMessageBox.confirm(
                '登录已过期，请重新登录',
                '提示',
                { confirmButtonText: '重新登录', type: 'warning' }
            ).then(() => {
                const userStore = useUserStore();
                userStore.logout();
                window.location.href = '/login';
            });
            break;
        case 403:
            ElMessage.error('没有操作权限');
            break;
        case 404:
            ElMessage.error('请求的资源不存在');
            break;
        default:
            ElMessage.error(data.message || '请求失败');
    }
};

// HTTP 错误处理
const handleHttpError = (error: AxiosError<ErrorResponse>): void => {
    if (error.response) {
        const { status, data } = error.response;
        
        switch (status) {
            case 400:
                ElMessage.error(data?.message || '请求参数错误');
                break;
            case 401:
                ElMessage.error('未授权，请登录');
                break;
            case 403:
                ElMessage.error('拒绝访问');
                break;
            case 404:
                ElMessage.error('请求地址不存在');
                break;
            case 408:
                ElMessage.error('请求超时');
                break;
            case 500:
                ElMessage.error('服务器内部错误');
                break;
            case 502:
                ElMessage.error('网关错误');
                break;
            case 503:
                ElMessage.error('服务不可用');
                break;
            case 504:
                ElMessage.error('网关超时');
                break;
            default:
                ElMessage.error(`连接错误 ${status}`);
        }
    } else if (error.request) {
        if (error.code === 'ECONNABORTED') {
            ElMessage.error('请求超时，请稍后重试');
        } else {
            ElMessage.error('网络异常，请检查网络连接');
        }
    } else {
        ElMessage.error('请求配置错误');
    }
};

// 生成 TraceId
const generateTraceId = (): string => {
    return `${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
};

// 取消所有请求
export const cancelAllRequests = (): void => {
    pendingMap.forEach(controller => controller.abort());
    pendingMap.clear();
};

export default request;
```

### 2.2 API 模块封装

```typescript
// api/user.ts
import request from '@/utils/request';
import type { User, CreateUserDTO, UpdateUserDTO, PageResponse } from '@/types';

export const userApi = {
    // 获取用户列表
    getList(params: { page: number; size: number; keyword?: string }) {
        return request.get<PageResponse<User>>('/users', { params });
    },
    
    // 获取用户详情
    getById(id: string) {
        return request.get<User>(`/users/${id}`);
    },
    
    // 创建用户
    create(data: CreateUserDTO) {
        return request.post<User>('/users', data);
    },
    
    // 更新用户
    update(id: string, data: UpdateUserDTO) {
        return request.put<User>(`/users/${id}`, data);
    },
    
    // 部分更新
    patch(id: string, data: Partial<UpdateUserDTO>) {
        return request.patch<User>(`/users/${id}`, data);
    },
    
    // 删除用户
    delete(id: string) {
        return request.delete<void>(`/users/${id}`);
    },
    
    // 批量删除
    batchDelete(ids: string[]) {
        return request.post<void>('/users/batch-delete', { ids });
    },
    
    // 导出用户
    export(params: { keyword?: string }) {
        return request.get<Blob>('/users/export', {
            params,
            responseType: 'blob'
        });
    },
    
    // 上传头像
    uploadAvatar(file: File) {
        const formData = new FormData();
        formData.append('file', file);
        return request.post<{ url: string }>('/users/avatar', formData, {
            headers: {
                'Content-Type': 'multipart/form-data'
            }
        });
    }
};

// 在组件中使用
<script setup lang="ts">
import { userApi } from '@/api/user';

const fetchUsers = async () => {
    try {
        const data = await userApi.getList({ page: 1, size: 20 });
        console.log(data.list);
    } catch (error) {
        // 错误已在拦截器处理
    }
};
</script>
```

## 三、Mock 方案

### 3.1 MSW (Mock Service Worker)

```typescript
// mocks/handlers.ts
import { rest } from 'msw';
import type { User, PageResponse } from '@/types';

const users: User[] = [
    { id: '1', name: '张三', email: 'zhangsan@example.com', status: 'active' },
    { id: '2', name: '李四', email: 'lisi@example.com', status: 'inactive' }
];

export const handlers = [
    // 获取用户列表
    rest.get('/api/users', (req, res, ctx) => {
        const page = Number(req.url.searchParams.get('page')) || 1;
        const size = Number(req.url.searchParams.get('size')) || 20;
        const keyword = req.url.searchParams.get('keyword');
        
        let filteredUsers = users;
        if (keyword) {
            filteredUsers = users.filter(u => 
                u.name.includes(keyword) || u.email.includes(keyword)
            );
        }
        
        const start = (page - 1) * size;
        const end = start + size;
        const pageData = filteredUsers.slice(start, end);
        
        const response: PageResponse<User> = {
            list: pageData,
            total: filteredUsers.length,
            page,
            size,
            pages: Math.ceil(filteredUsers.length / size)
        };
        
        return res(
            ctx.delay(500), // 模拟网络延迟
            ctx.status(200),
            ctx.json({
                code: 200,
                message: 'success',
                data: response,
                timestamp: Date.now(),
                traceId: 'mock-trace-id'
            })
        );
    }),
    
    // 获取用户详情
    rest.get('/api/users/:id', (req, res, ctx) => {
        const { id } = req.params;
        const user = users.find(u => u.id === id);
        
        if (!user) {
            return res(
                ctx.status(404),
                ctx.json({
                    code: 404,
                    message: '用户不存在',
                    timestamp: Date.now()
                })
            );
        }
        
        return res(
            ctx.status(200),
            ctx.json({
                code: 200,
                message: 'success',
                data: user,
                timestamp: Date.now()
            })
        );
    }),
    
    // 创建用户
    rest.post('/api/users', async (req, res, ctx) => {
        const body = await req.json();
        const newUser: User = {
            id: String(users.length + 1),
            ...body,
            status: 'active'
        };
        users.push(newUser);
        
        return res(
            ctx.status(201),
            ctx.json({
                code: 201,
                message: '创建成功',
                data: newUser,
                timestamp: Date.now()
            })
        );
    }),
    
    // 模拟错误
    rest.get('/api/error', (req, res, ctx) => {
        return res(
            ctx.status(500),
            ctx.json({
                code: 500,
                message: '服务器内部错误',
                timestamp: Date.now(),
                traceId: 'error-trace-id'
            })
        );
    })
];
```

```typescript
// mocks/browser.ts
import { setupWorker } from 'msw';
import { handlers } from './handlers';

export const worker = setupWorker(...handlers);
```

```typescript
// main.ts - 启动 Mock
async function enableMocking() {
    if (process.env.NODE_ENV !== 'development') {
        return;
    }
    
    const { worker } = await import('./mocks/browser');
    return worker.start({
        onUnhandledRequest: 'bypass' // 未匹配的请求直接透传
    });
}

enableMocking().then(() => {
    app.mount('#app');
});
```

### 3.2 Vite 插件 Mock

```typescript
// vite.config.ts
import { viteMockServe } from 'vite-plugin-mock';

export default defineConfig({
    plugins: [
        viteMockServe({
            mockPath: 'mock',
            localEnabled: true,
            prodEnabled: false,
            injectCode: `
                import { setupProdMockServer } from './mockProdServer';
                setupProdMockServer();
            `
        })
    ]
});
```

```typescript
// mock/user.ts
import { MockMethod } from 'vite-plugin-mock';

export default [
    {
        url: '/api/users',
        method: 'get',
        response: () => {
            return {
                code: 200,
                message: 'success',
                data: {
                    list: [
                        { id: '1', name: '张三' },
                        { id: '2', name: '李四' }
                    ],
                    total: 2
                }
            };
        }
    },
    {
        url: '/api/users',
        method: 'post',
        response: ({ body }) => {
            return {
                code: 201,
                message: '创建成功',
                data: {
                    id: '3',
                    ...body
                }
            };
        }
    }
] as MockMethod[];
```

## 四、认证与授权

### 4.1 JWT 认证流程

```
┌─────────────┐                    ┌─────────────┐
│   前端      │ ─── 1.登录请求 ───> │   后端      │
│             │  {username, pwd}   │             │
│             │ <── 2.返回 Token ── │  验证身份   │
│  存储 Token │    {access, refresh} │  生成 JWT   │
│             │                    │             │
│             │ ─── 3.请求 API ───> │             │
│  携带 Token │  Authorization:    │  验证 Token │
│             │  Bearer <token>    │             │
│             │ <── 4.返回数据 ──── │             │
│             │                    │             │
│             │ ─── 5.Token过期 ──> │             │
│             │ <── 6.401错误 ───── │             │
│             │                    │             │
│  刷新 Token │ ─── 7.刷新请求 ───> │  验证refresh │
│             │  {refreshToken}    │  生成新Token │
│             │ <── 8.新Token ───── │             │
│             │                    │             │
└─────────────┘                    └─────────────┘
```

### 4.2 Token 刷新机制

```typescript
// utils/request.ts - Token 刷新
let isRefreshing = false;
let refreshSubscribers: ((token: string) => void)[] = [];

// 订阅 Token 刷新
const subscribeTokenRefresh = (callback: (token: string) => void) => {
    refreshSubscribers.push(callback);
};

// 通知所有订阅者
const onTokenRefreshed = (newToken: string) => {
    refreshSubscribers.forEach(callback => callback(newToken));
    refreshSubscribers = [];
};

// 刷新 Token
const refreshToken = async (): Promise<string> => {
    const refreshToken = localStorage.getItem('refreshToken');
    const response = await axios.post('/api/auth/refresh', { refreshToken });
    const { accessToken, refreshToken: newRefreshToken } = response.data.data;
    
    localStorage.setItem('accessToken', accessToken);
    localStorage.setItem('refreshToken', newRefreshToken);
    
    return accessToken;
};

// 响应拦截器中处理 401
request.interceptors.response.use(
    (response) => response,
    async (error: AxiosError) => {
        const originalRequest = error.config as AxiosRequestConfig & { _retry?: boolean };
        
        if (error.response?.status === 401 && !originalRequest._retry) {
            if (isRefreshing) {
                // 正在刷新，等待新 Token
                return new Promise((resolve) => {
                    subscribeTokenRefresh((newToken: string) => {
                        originalRequest.headers = originalRequest.headers || {};
                        originalRequest.headers.Authorization = `Bearer ${newToken}`;
                        resolve(request(originalRequest));
                    });
                });
            }
            
            originalRequest._retry = true;
            isRefreshing = true;
            
            try {
                const newToken = await refreshToken();
                onTokenRefreshed(newToken);
                originalRequest.headers = originalRequest.headers || {};
                originalRequest.headers.Authorization = `Bearer ${newToken}`;
                return request(originalRequest);
            } catch (refreshError) {
                // 刷新失败，跳转登录
                const userStore = useUserStore();
                userStore.logout();
                window.location.href = '/login';
                return Promise.reject(refreshError);
            } finally {
                isRefreshing = false;
            }
        }
        
        return Promise.reject(error);
    }
);
```

## 五、错误处理最佳实践

### 5.1 前端错误分类处理

```typescript
// types/error.ts
export enum ErrorType {
    NETWORK = 'NETWORK',           // 网络错误
    TIMEOUT = 'TIMEOUT',           // 超时错误
    SERVER = 'SERVER',             // 服务器错误
    BUSINESS = 'BUSINESS',         // 业务错误
    VALIDATION = 'VALIDATION',     // 验证错误
    AUTH = 'AUTH',                 // 认证错误
    PERMISSION = 'PERMISSION',     // 权限错误
    UNKNOWN = 'UNKNOWN'            // 未知错误
}

export interface AppError {
    type: ErrorType;
    message: string;
    code?: number;
    traceId?: string;
    originalError?: any;
}

// utils/errorHandler.ts
export const errorHandler = {
    // 分类错误
    classify(error: AxiosError | Error): AppError {
        if (axios.isAxiosError(error)) {
            if (error.code === 'ECONNABORTED') {
                return {
                    type: ErrorType.TIMEOUT,
                    message: '请求超时，请稍后重试',
                    originalError: error
                };
            }
            
            if (!error.response) {
                return {
                    type: ErrorType.NETWORK,
                    message: '网络连接失败，请检查网络',
                    originalError: error
                };
            }
            
            const { status, data } = error.response;
            
            switch (status) {
                case 400:
                    return {
                        type: ErrorType.VALIDATION,
                        message: data?.message || '请求参数错误',
                        code: status,
                        traceId: data?.traceId,
                        originalError: error
                    };
                case 401:
                    return {
                        type: ErrorType.AUTH,
                        message: '登录已过期，请重新登录',
                        code: status,
                        originalError: error
                    };
                case 403:
                    return {
                        type: ErrorType.PERMISSION,
                        message: '没有操作权限',
                        code: status,
                        originalError: error
                    };
                case 500:
                case 502:
                case 503:
                case 504:
                    return {
                        type: ErrorType.SERVER,
                        message: '服务器繁忙，请稍后重试',
                        code: status,
                        traceId: data?.traceId,
                        originalError: error
                    };
                default:
                    return {
                        type: ErrorType.UNKNOWN,
                        message: '发生未知错误',
                        code: status,
                        originalError: error
                    };
            }
        }
        
        return {
            type: ErrorType.UNKNOWN,
            message: error.message || '发生未知错误',
            originalError: error
        };
    },
    
    // 统一处理
    handle(error: AxiosError | Error): void {
        const appError = this.classify(error);
        
        // 上报监控
        this.report(appError);
        
        // 根据类型处理
        switch (appError.type) {
            case ErrorType.AUTH:
                // 跳转登录
                break;
            case ErrorType.PERMISSION:
                // 显示无权限提示
                ElMessage.warning(appError.message);
                break;
            case ErrorType.VALIDATION:
                // 表单验证错误，已在组件处理
                break;
            default:
                ElMessage.error(appError.message);
        }
    },
    
    // 错误上报
    report(error: AppError): void {
        // 发送到监控平台（Sentry/阿里云ARMS等）
        if (process.env.NODE_ENV === 'production') {
            console.log('Report error:', error);
        }
    }
};
```

---

## 六、高频面试题

**问题 1：如何处理前端请求并发和竞态问题？**

**答：**

```typescript
// 1. 使用 AbortController 取消请求
const controller = new AbortController();
fetch('/api/data', { signal: controller.signal });
controller.abort(); // 取消请求

// 2. 使用唯一标识符忽略过期请求
let requestId = 0;

async function fetchData() {
    const currentRequestId = ++requestId;
    const response = await fetch('/api/data');
    const data = await response.json();
    
    // 只处理最新请求的结果
    if (currentRequestId === requestId) {
        setData(data);
    }
}

// 3. 使用防抖/节流
import { debounce, throttle } from 'lodash-es';

const debouncedSearch = debounce((keyword) => {
    fetchSearchResults(keyword);
}, 300);
```

---

**问题 2：如何设计一个可扩展的 API 层？**

**答：**

```typescript
// 分层设计
// 1. 基础请求层（request.ts）- 处理通用逻辑
// 2. API 模块层（api/*.ts）- 按业务模块组织
// 3. 类型定义层（types/*.ts）- 统一管理类型
// 4. Mock 层（mocks/*.ts）- 开发环境模拟

// 关键原则：
// - 单一职责：每个 API 模块只处理一类资源
// - 类型安全：全链路 TypeScript 类型
// - 错误统一：拦截器统一处理错误
// - 可测试：Mock 层支持单元测试
```

---

**问题 3：前后端如何约定接口版本？**

**答：**

| 方案           | URL 示例                                | 优点     | 缺点     |
|--------------|---------------------------------------|--------|--------|
| **URL Path** | `/api/v1/users`                       | 直观、易缓存 | URL 变动 |
| **Header**   | `Accept: application/vnd.api.v1+json` | URL 稳定 | 不够直观   |
| **Query**    | `/api/users?version=1`                | 灵活     | 不够规范   |

**推荐 URL Path 方案：**

```typescript
// 前端配置
const API_VERSION = 'v1';
const baseURL = `/api/${API_VERSION}`;

// 版本切换
const request = axios.create({
    baseURL: import.meta.env.VITE_API_VERSION === 'v2' 
        ? '/api/v2' 
        : '/api/v1'
});
```

---

**问题 4：如何处理大文件上传？**

**答：**

```typescript
// 分片上传
async function uploadLargeFile(file: File) {
    const chunkSize = 5 * 1024 * 1024; // 5MB
    const chunks = Math.ceil(file.size / chunkSize);
    const uploadId = generateUploadId();

    // 1. 初始化上传
    await request.post('/upload/init', {
        filename: file.name,
        size: file.size,
        chunks
    });

    // 2. 并行上传分片
    const uploadPromises = [];
    for (let i = 0; i < chunks; i++) {
        const start = i * chunkSize;
        const end = Math.min(start + chunkSize, file.size);
        const chunk = file.slice(start, end);

        const formData = new FormData();
        formData.append('chunk', chunk);
        formData.append('index', String(i));
        formData.append('uploadId', uploadId);

        uploadPromises.push(
            request.post('/upload/chunk', formData, {
                onUploadProgress: (progress) => {
                    // 更新进度
                }
            })
        );
    }

    await Promise.all(uploadPromises);

    // 3. 合并分片
    await request.post('/upload/merge', {uploadId});
}
```

---

**问题 5：如何防止重复提交？**

**答：**

```typescript
// 方案 1：按钮 loading 状态
const [submitting, setSubmitting] = useState(false);

const handleSubmit = async () => {
    if (submitting) return;
    setSubmitting(true);
    try {
        await submitForm();
    } finally {
        setSubmitting(false);
    }
};

// 方案 2：请求去重（拦截器实现）
// 见上文 request.ts 中的 pendingMap 实现

// 方案 3：幂等性 Token
const handleSubmit = async () => {
    const idempotencyKey = generateUUID();
    await request.post('/api/order', data, {
        headers: { 'Idempotency-Key': idempotencyKey }
    });
};
```
