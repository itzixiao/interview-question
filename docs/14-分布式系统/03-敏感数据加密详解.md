# 敏感数据加密详解

## 概述

敏感数据加密是保护用户隐私数据的核心手段，涉及密码存储、证件加密、数据脱敏等多个方面。

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                           敏感数据加密架构图                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐                     │
│    │   密码存储    │    │   证件加密   │    │   数据脱敏   │                     │
│    │   BCrypt    │    │   AES-GCM   │    │   注解驱动   │                     │
│    └──────┬──────┘    └──────┬──────┘    └──────┬──────┘                     │
│           │                  │                  │                            │
│           └──────────────────┼──────────────────┘                            │
│                              │                                               │
│                       ┌──────▼────────┐                                      │
│                       │   KMS/配置中心 │                                      │
│                       │    密钥管理    │                                      │
│                       └───────────────┘                                      │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 1. 敏感数据分类

| 类型  | 处理方式      | 示例      |
|-----|-----------|---------|
| 密码类 | 哈希存储，永不解密 | 登录密码    |
| 证件类 | 可逆加密，需要查询 | 身份证、银行卡 |
| 展示类 | 脱敏显示      | 手机号、邮箱  |

---

## 2. 加密算法选型

| 算法                  | 特点           | 适用场景      |
|---------------------|--------------|-----------|
| **MD5/SHA-256 + 盐** | 哈希不可逆，加盐防彩虹表 | 密码存储      |
| **BCrypt/Argon2**   | 专用密码哈希，自带盐   | 密码存储（推荐）  |
| **AES-256-GCM**     | 对称加密，高性能     | 身份证、银行卡加密 |
| **RSA-2048**        | 非对称，安全但慢     | 密钥交换、数字签名 |

---

## 3. AES-GCM加密实现

```java
public class AesGcmUtil {
    
    // 加密
    public static String encrypt(String plainText, SecretKey secretKey) throws Exception {
        Cipher cipher = Cipher.getInstance("AES/GCM/NoPadding");
        
        // 生成12字节随机IV
        byte[] iv = new byte[12];
        new SecureRandom().nextBytes(iv);
        
        GCMParameterSpec parameterSpec = new GCMParameterSpec(128, iv);
        cipher.init(Cipher.ENCRYPT_MODE, secretKey, parameterSpec);
        
        byte[] encrypted = cipher.doFinal(plainText.getBytes(StandardCharsets.UTF_8));
        
        // IV + 密文一起存储
        byte[] result = new byte[iv.length + encrypted.length];
        System.arraycopy(iv, 0, result, 0, iv.length);
        System.arraycopy(encrypted, 0, result, iv.length, encrypted.length);
        
        return Base64.getEncoder().encodeToString(result);
    }
    
    // 解密
    public static String decrypt(String encryptedText, SecretKey secretKey) throws Exception {
        byte[] decoded = Base64.getDecoder().decode(encryptedText);
        
        // 分离IV和密文
        byte[] iv = Arrays.copyOfRange(decoded, 0, 12);
        byte[] encrypted = Arrays.copyOfRange(decoded, 12, decoded.length);
        
        Cipher cipher = Cipher.getInstance("AES/GCM/NoPadding");
        GCMParameterSpec parameterSpec = new GCMParameterSpec(128, iv);
        cipher.init(Cipher.DECRYPT_MODE, secretKey, parameterSpec);
        
        return new String(cipher.doFinal(encrypted), StandardCharsets.UTF_8);
    }
}
```

---

## 4. 混合加密方案（推荐）

```
┌─────────────────────────────────────────────────────────────┐
│  1. 客户端生成随机 AES 密钥                                  │
│  2. 用 AES 密钥加密业务数据（高效）                          │
│  3. 用服务端 RSA 公钥加密 AES 密钥（安全）                   │
│  4. 发送: RSA(AES_KEY) + AES(DATA)                          │
│  5. 服务端用 RSA 私钥解密得到 AES 密钥                       │
│  6. 用 AES 密钥解密业务数据                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. 数据脱敏

### 5.1 脱敏规则

| 数据类型 | 原始数据                 | 脱敏结果               |
|------|----------------------|--------------------|
| 手机号  | 13800138000          | 138****8000        |
| 身份证  | 110101199001011234   | 110101********1234 |
| 银行卡  | 6222021234567890123  | 6222****0123       |
| 邮箱   | zhangsan@example.com | z***n@example.com  |
| 姓名   | 张三丰                  | 张**                |

### 5.2 注解实现脱敏

```java
public class UserVO {
    @Sensitive(type = SensitiveType.PHONE)
    private String phone;
    
    @Sensitive(type = SensitiveType.ID_CARD)
    private String idCard;
}

// Jackson序列化时自动脱敏
// 实现 JsonSerializer<String>
```

---

## 6. 高频面试题

**问题 1：AES和RSA的区别？实际项目中如何选择？**

**答：**

| 维度   | AES（对称加密） | RSA（非对称加密） |
|------|-----------|------------|
| 密钥   | 加解密同一密钥   | 公钥加密，私钥解密  |
| 性能   | 快，适合大数据量  | 慢，适合小数据    |
| 密钥管理 | 密钥需安全传输   | 公钥可公开      |
| 典型场景 | 数据库字段加密   | 密钥交换、数字签名  |

**实际项目推荐「混合加密」**：

1. 用RSA加密AES密钥（安全传输）
2. 用AES加密业务数据（高效处理）

---

**问题 2：为什么密码要加盐？彩虹表攻击是什么？**

**答：**

彩虹表是预先计算好的「明文→哈希值」对照表。攻击者获取数据库后，直接查表就能反推原始密码。

加盐原理：

```
原始: hash = SHA256(password)          → 可查表破解
加盐: hash = SHA256(password + salt)   → 彩虹表失效
```

最佳实践：

1. 每个用户使用不同的随机盐值
2. 盐值和哈希一起存储: salt:hash
3. 推荐 BCrypt/Argon2，自带盐和计算成本

---

**问题 3：AES的ECB和GCM模式有什么区别？**

**答：**

**ECB（电子密码本模式）**：

- 相同明文块加密出相同密文块
- 可通过密文模式推测明文结构
- **不安全，不推荐使用**

**GCM（Galois/Counter模式）**：

- 每次加密使用随机IV，相同明文密文不同
- 自带认证标签（Authentication Tag），可检测篡改
- **推荐使用，HTTPS/TLS默认采用**

---

**问题 4：如何实现接口级别的数据脱敏？**

**答：**

推荐使用自定义注解 + Jackson序列化器：

1. 定义脱敏注解 `@Sensitive`
2. 定义脱敏类型枚举：PHONE/ID_CARD/BANK_CARD/EMAIL
3. 实现 `JsonSerializer<String>`，根据类型脱敏
4. VO字段加注解，返回时自动脱敏

优点：

- 统一管理脱敏规则
- 字段级别控制
- 与业务代码解耦

---

**问题 5：分布式环境下如何保证密钥安全？**

**答：**

1. **密钥管理服务（KMS）**
    - 使用阿里云KMS、AWS KMS等托管服务
    - 密钥不落地，由云服务管理

2. **配置中心加密**
    - Nacos/Apollo支持配置加密
    - 密钥从配置中心动态获取

3. **环境变量注入**
    - K8s Secret、Docker Secret
    - 启动时注入，不写入代码和配置文件

4. **密钥轮换**
    - 定期更换密钥
    - 支持新旧密钥并存过渡期

---

## 7. 相关代码示例

完整代码示例请参考：

- [DistributedSecurityPrincipleDemo.java](../../interview-microservices-parent/interview-service/src/main/java/cn/itzixiao/interview/security/DistributedSecurityPrincipleDemo.java)
- [RedisSecurityDemo.java](../../interview-microservices-parent/interview-service/src/main/java/cn/itzixiao/interview/redis/advanced/RedisSecurityDemo.java)
